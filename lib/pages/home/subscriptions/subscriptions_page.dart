import 'package:flutter/material.dart';
import './body.dart';

class SubscriptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buy Subscripiton'),
      ),
      body: Body(),
    );
  }
  
}
